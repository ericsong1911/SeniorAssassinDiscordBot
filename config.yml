bot:
  token: YOUR_BOT_TOKEN
  prefix: '!'

game:
  min_players: 4
  max_players: 20
  team_creation_timeout: 30000
  join_request_timeout: 86400000
  announcement_channel_id: ANNOUNCEMENT_CHANNEL_ID
  notification_channel_id: NOTIFICATION_CHANNEL_ID

game_manager:
  role_id: GAME_MANAGER_ROLE_ID
  channel_id: GAME_MANAGER_CHANNEL_ID
  leaderboard_message_id: LEADERBOARD_MESSAGE_ID

database:
  dialect: sqlite
  storage: assassin.sqlite